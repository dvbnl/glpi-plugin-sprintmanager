{
    "name": "dvbnl/glpi-plugin-sprintmanager",
    "description": "Agile/Scrum Sprint Management Plugin for GLPI - Link tickets, changes, and project tasks to sprints with capacity tracking and meeting management",
    "type": "glpi-plugin",
    "license": "GPL-3.0-or-later",
    "authors": [
        {
            "name": "DVBNL"
        }
    ],
    "require": {
        "php": ">=8.1"
    },
    "autoload": {
        "psr-4": {
            "GlpiPlugin\\Sprint\\": "src/"
        }
    },
    "scripts": {
        "lint": "find src front ajax -name '*.php' -print0 | xargs -0 -n1 php -l",
        "test": [
            "php tests/static_contracts.php",
            "php tests/inline_js_syntax.php"
        ]
    }
}
